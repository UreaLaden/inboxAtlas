version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused

  settings:
    staticcheck:
      checks:
        - "all"
        - "-ST1000" # package comment not required on every file

formatters:
  enable:
    - gofmt
